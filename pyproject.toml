# pyproject.toml

[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "interpol_utils"
version = "1.0.0"
description = "tbd"
readme = "README.md"
license = { file = "LICENSE" }
dependencies = []
